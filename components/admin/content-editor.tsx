"use client";
import { useState } from "react";
const initial=[{label:"Dòng giới thiệu",key:"home.hero.eyebrow",value:"Ở chậm giữa thiên nhiên"},{label:"Tiêu đề trang chủ",key:"home.hero.title",value:"Một khoảng xanh để mình thật sự nghỉ ngơi"},{label:"Mô tả trang chủ",key:"home.hero.description",value:"Lago là nơi những ngày dài được đo bằng nắng sớm, bữa cơm ấm và thời gian dành trọn cho nhau."}];
export function ContentEditor(){const [saved,setSaved]=useState(false);return <div className="card mt-8 p-6"><div className="grid gap-6">{initial.map(item=><label className="text-sm font-bold" key={item.key}>{item.label}<span className="ml-2 font-mono text-[.65rem] font-normal text-lago-ink/35">{item.key}</span>{item.value.length>70?<textarea defaultValue={item.value} className="input mt-2 h-28 py-3"/>:<input defaultValue={item.value} className="input mt-2"/>}</label>)}</div><div className="mt-7 flex items-center gap-4"><button onClick={()=>{setSaved(true);setTimeout(()=>setSaved(false),2500)}} className="btn-primary">Lưu thay đổi</button>{saved&&<span className="text-sm font-bold text-emerald-700">Đã lưu nội dung</span>}</div></div>}
